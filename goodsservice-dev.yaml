server:
  port: 32200
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/goods-db
  jpa:
    database: POSTGRESQL
    generate-ddl: true
    show-sql: true
  application:
    name: goods-service