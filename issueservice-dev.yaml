server:
  port: 32203
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:6668/issue-db
  jpa:
    database: POSTGRESQL
    generate-ddl: true
    show-sql: true
  application:
    name: issue-service
